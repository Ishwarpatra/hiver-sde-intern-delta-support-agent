[project]
name = "delta-support-agent"
version = "0.1.0"
dependencies = ["pandas>=2.0", "scikit-learn>=1.3"]
requires-python = ">=3.10"

[tool.setuptools]
packages = ["delta_agent"]

[project.scripts]
delta-agent = "delta_agent.cli:main"
